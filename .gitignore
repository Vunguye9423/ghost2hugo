# ── Python ──────────────────────────────────────────────────────────
.venv/
venv/
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.mypy_cache/
build/
dist/

# ── Secrets & personal data — NEVER commit ─────────────────────────
# Anchored to the repo root so the test fixture (tests/fixtures/config.yaml)
# is still tracked.
/config.yaml
/exclude.txt
*.ghost.*.json
ghost-export*.json

# ── Migration run artifacts (regenerated each run) ─────────────────
.migration-state.json
migration-report.md
verification-report.md
state/
quarantine/
.verify-screenshots/

# ── Test output (regenerated by tests/run_e2e.sh) ──────────────────
tests/out/

# ── OS ──────────────────────────────────────────────────────────────
.DS_Store
Thumbs.db
